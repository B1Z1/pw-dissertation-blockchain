{
  "name": "dessertation-blockchain-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/dessertation-blockchain-ui",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/dessertation-blockchain-ui"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/dessertation-blockchain-ui"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "dessertation-blockchain-ui:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "dessertation-blockchain-ui:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "dessertation-blockchain-ui:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "dessertation-blockchain-ui:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/dessertation-blockchain-ui/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "apps/dessertation-blockchain-ui/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
